# Integration tests

These tests will exercise the full FastAPI routes end to end
(health check, predict, batch predict) once the API is built in
Task 3's step 7.

They are intentionally empty for now — testing routes that don't
exist yet isn't possible. This step (6) covers unit, data, and
model tests; integration tests are completed as part of step 7.
